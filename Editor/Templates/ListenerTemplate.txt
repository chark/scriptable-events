using UnityEngine;

namespace ${LISTENER_NAMESPACE}
{
    [AddComponentMenu(
        ScriptableEventConstants.MenuNamePrefix + "/${LISTENER_MENU_NAME}",
        ScriptableEventConstants.MenuOrderCustomEvent + ${LISTENER_MENU_ORDER}
    )]
    public class ${LISTENER_NAME} : BaseScriptableEventListener<${EVENT_ARG_NAME}>
    {
    }
}
