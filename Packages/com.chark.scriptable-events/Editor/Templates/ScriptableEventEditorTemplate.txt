using EVENT_NAMESPACE;
using UnityEditor;

namespace EDITOR_NAMESPACE
{
    [CustomEditor(typeof(EVENT_NAME))]
    public class EDITOR_NAME : ScriptableEvents.Events.BaseScriptableEventEditor<EVENT_ARG_FULL_NAME>
    {
        protected override EVENT_ARG_FULL_NAME DrawArgField(EVENT_ARG_FULL_NAME value)
        {
            // Use EditorGUILayout.TextField, etc., to draw inputs next to Raise button on your
            // EVENT_NAME asset.
            return value;
        }
    }
}
